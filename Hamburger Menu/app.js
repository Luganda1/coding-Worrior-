document.querySelector('.hamburger-menu').addEventListener('click', () => {
    document.querySelector('.nav-wrapper').classList.toggle('change');
    // classList same like cssText in the other exaples has attributes we can use 
}) 